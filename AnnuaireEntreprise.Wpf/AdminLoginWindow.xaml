<Window x:Class="AnnuaireEntreprise.AdminLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Authentification administrateur" Height="210" Width="420" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Saisir le mot de passe administrateur :" Margin="0,0,0,10"/>
        <PasswordBox x:Name="PasswordInput" Grid.Row="1" Height="30"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Annuler" Width="90" Margin="0,0,8,0" Click="OnCancel"/>
            <Button Content="Valider" Width="90" IsDefault="True" Click="OnValidate"/>
        </StackPanel>
    </Grid>
</Window>
